{
  "name": "explore-topics",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "get": "cdktf get",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "upgrade": "npm i cdktf@latest cdktf-cli@latest",
    "upgrade:next": "npm i cdktf@next cdktf-cli@next",
    "lint:check": "eslint --fix-dry-run src/**/*.ts",
    "lint:fix": "eslint --fix src/**/*.ts"
  },
  "engines": {
    "node": ">=10.12"
  },
  "dependencies": {
    "@pocket/terraform-modules": "1.3.5",
    "cdktf": "0.0.17",
    "constructs": "^3.2.10"
  },
  "devDependencies": {
    "@types/node": "14.14.6",
    "cdktf-cli": "0.0.17",
    "typescript": "^4.0.5"
  }
}
