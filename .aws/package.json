{
  "name": "explore-topics",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "get": "cdktf get",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "upgrade": "npm i cdktf@latest cdktf-cli@latest",
    "upgrade:next": "npm i cdktf@next cdktf-cli@next"
  },
  "engines": {
    "node": ">=10.12"
  },
  "dependencies": {
    "@pocket/terraform-modules": "1.2.11",
    "cdktf": "0.0.16",
    "constructs": "^3.0.4"
  },
  "devDependencies": {
    "@types/node": "14.6.1",
    "cdktf-cli": "0.0.16",
    "typescript": "^4.0.2"
  }
}
